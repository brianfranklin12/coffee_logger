class Log {
  constructor({ id, dose, yield, grindSize, brewTime, brewMethod, rating, notes, coffeeID }) {
    this.id = id;
    this.dose = dose;
    this.yield = yield;
    this.grindSize = grindSize;
    this.brewTime = brewTime;
    this.brewMethod = brewMethod;
    this.rating = rating;
    this.notes = notes;
    this.coffeeID = coffeeID;
  }
}